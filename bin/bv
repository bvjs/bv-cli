#!/usr/bin/env node

const program = require('commander')
const packageConfig = require('../package')

program
  .version(packageConfig.version)
  .usage('<command> [options]')
  .command('init', '新建项目')
  .command('list', '查看可用模板列表')
  .command('build', '打包')
  .parse(process.argv)
